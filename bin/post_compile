#!/usr/bin/env bash
# Heroku Python buildpack calls this script after pip install, during the BUILD phase.
# Files created here are baked into the slug and available to all dynos.

set -e

echo "-----> Running collectstatic"
python manage.py collectstatic --noinput

echo "-----> collectstatic done"
